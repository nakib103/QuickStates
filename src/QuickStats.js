import React, {Component} from "react"
import ReactDOM from "react-dom"
import Uva from "./Cards/Uva"

class QuickStates extends Component {
    render = () => {
        return (
            <Uva userID={345}/>
        )
    }
}

export default QuickStates